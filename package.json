{
  "name": "project-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd mongoosetest && npm run build",
    "install-client": "cd mongoosetest && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "devStart": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.6.2",
    "dotenv": "^16.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}